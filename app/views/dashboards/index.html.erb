<div class="row">
  <div class="col-xs-12">
    <style media="screen">
      .company { float: left; width: 30%; margin: 1em 1.5%; text-decoration: none; }
      .company span { display: inline-block; margin-top: 0.5em; color: gray; -webkit-transition: all 0.3s; -moz-transition: all 0.3s; transition: all 0.3s; }
      .company img { width: 80%; -webkit-filter: grayscale(100%); -moz-filter: grayscale(100%); filter: grayscale(100%); -webkit-transition: all 0.3s; -moz-transition: all 0.3s; transition: all 0.3s; }
      .company:hover img { -webkit-filter: grayscale(0); -moz-filter: grayscale(0); filter: grayscale(0); }
      .company:hover span { color: blue; }
      @media screen and (max-width: 600px) { .company { width: 47%; } }
    </style>
    <h2>Companies that React!</h2>
    <!-- we'll throw a bunch of logos here -->
    <div id="show-companies"></div>
  </div>
</div>

<% content_for :header do %>
  <%= render 'shared/menu' %>
<% end %>

<% content_for :side do %>
  <%= render 'shared/side' %>
<% end %>

<% content_for :content do %>  
  <div class="main uk-section">
    <div class="uk-container uk-container-expand uk-position-relative uk-padding-large">
      <div class="uk-grid">
        <div class="uk-width-1-2 ">
          <span style=" color: #000 !important;">TOTAL EARNINGS - Sample only</span>
          <h2 class="uk-margin-remove-top uk-text-bold">PHP 100.00</h2>
        </div>
        <div class="uk-width-1-2 ">
          <div class="uk-align-right uk-margin-small-right">
          <span style="color: #000 !important;">PAY PERIOD</span>
          <br>
          <select class="uk-form-select" data-uk-form-select>
              <option value="">Current Statement</option>
              <option value="">Previous Statement</option>
          </select>
          </div>
        </div>
      </div>

      <div class="uk-grid uk-margin-remove-left bordered">
        <div class="uk-width-1-4 bordered-right">
          <h3 class="uk-margin-remove-bottom bookingfont">0</h3>
          <span>PAID BOOKINGS</span>
        </div>
        <div class="uk-width-1-4 bordered-right">
          <h3 class="uk-margin-remove-bottom">0</h3>
          <span>RESERVED BOOKINGS</span>
        </div>
        <div class="uk-width-1-4 bordered-right">
          <h3 class="uk-margin-remove-bottom">0</h3>
          <span>BOOKING CANCELATIONS</span>
        <div class="uk-width-1-4">
        </div>
        </div>
      </div>
        <div class="uk-grid uk-margin-small">
          <h3>Weekly Earnings</h3>
        </div>
        <div class="uk-grid">
            <div class="uk-grid">
              <h4>Earnings</h4>
            </div>
            <div class="uk-grid">
              <h4>Earnings</h4>
            </div>
            <div class="uk-grid">
              <h4>Earnings</h4>
            </div>
        </div>
      </div>
    </div>
  </div>
<% end %>